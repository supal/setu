import { ResetPasswordForm } from "./ResetPasswordForm";

export function SetPassword() {
  return <ResetPasswordForm title="Set your password" subtitle="Welcome to SiteTracker — choose a password to get started." />;
}
